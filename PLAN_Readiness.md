# Readiness 改版计划：5 档颜色主卡 + 动态白天活动分 + 夜间正交模型

## 摘要
本次改版保留白天/夜间双模式，但明确分成两套语义：

- 白天：表达“清醒状态 / 可用能量”，分数越高越好，颜色越偏蓝。
- 夜间：表达“睡眠压力 / 入睡需求”，分数越高越该收尾睡觉，颜色越偏红。

展示层改成单主卡结构，主体参考你给的 UI：一排由小到大的彩色圆点，大圆右侧全部灰色，表示后续档位尚未发生；数字分数只做小号辅助信息，不再做视觉中心。主卡下方只保留一句总解释，不保留当前三张因子卡。文案层改成“标题 + 副文案”两层，副文案采用“原因 + 建议”的拼装规则。

## 关键改动

### 1. 评分模型与 5 档映射

- 保留 `ReadinessMode.day` / `ReadinessMode.night` 两种模式，不合并成一个统一语义分数。
- 新增展示层档位模型，例如 `ReadinessBand`，固定 5 档，供 UI 和文案共同消费。
- 第一版统一使用这组区间：
  - `0...34`
  - `35...49`
  - `50...64`
  - `65...79`
  - `80...100`
- 同一档位在白天和夜间共用数值区间，但语义、颜色、标题词表分开。

### 2. 白天模式打分调整

- 白天总分仍然由 3 个因子构成，但活动分改成“时段目标曲线”，不再直接用全天累计步数静态分档。
- 白天建议改成：
  - 节律 `0.40`
  - 恢复 `0.40`
  - 活动 `0.20`
- 活动分 `A_day` 采用“累计进度 + 近期激活”双输入。
- 先定义白天 4 个时段目标步数：
  - `06:00-09:59`：目标 `300-1200`
  - `10:00-13:59`：目标 `1500-3500`
  - `14:00-17:59`：目标 `4000-7000`
  - `18:00-21:59`：目标 `6000-9000`
- 累计步数按“落在当前时段合理区间内最佳”评分：
  - 低于区间下限：线性扣分，但早晨不出现极低分
  - 落在区间内：高分区
  - 明显高于区间上限：轻微回落，不做重罚
- 近 2 小时步数仅做微调：
  - 极低活跃且当前为 `10:00` 之后：小幅减分
  - 极高活跃：小幅减分
  - `10:00` 前不因“近 2 小时很低”而扣分
- 目标效果：
  - 刚起床、上午步数少，不应被判成明显低分
  - 午后至傍晚，活动不足才逐步拉低状态
  - 白天活动分表达“激活状态是否合理”，不是“走得越多越好”

### 3. 夜间模式重构为正交模型

- 夜间不再沿用“恢复分 + Sleep Debt Bonus 外挂加分”的结构。
- 改成 3 个正交夜间因子：
  - `SleepPressure`：睡眠压力，核心由昨夜睡眠欠债决定
  - `CircadianNight`：夜间节律窗口是否形成
  - `Expenditure`：今日身体消耗是否到位
- 夜间建议权重：
  - 睡眠压力 `0.45`
  - 夜间节律 `0.35`
  - 今日消耗 `0.20`
- `SleepPressure` 直接替代当前 `Recovery + bonus` 的组合逻辑：
  - `>=8h`：低睡眠压力
  - `7-8h`：偏低
  - `6-7h`：中等
  - `5-6h`：偏高
  - `<5h`：高
- `CircadianNight` 继续复用现有夜间曲线和日出日落修正逻辑。
- `Expenditure` 使用今日累计步数或活动能量做饱和型评分：
  - 不足：低
  - 到位：高
  - 超额：封顶，不额外抬升
- 删除夜间总分上的 `SleepDebtBonus` 概念；如果要兼容旧调试面板，可保留字段但固定为 `0`，或在本次一并移除并同步调整类型。

### 4. 展示层改版

- 用新的主卡替换当前 `RingChartView` 主体。
- 主卡结构固定为：
  - 顶部小标题：当前状态表现
  - 中部 5 档圆点轨道：当前档位左侧彩色，当前档位最大，右侧全部灰色
  - 大标题：`状态判断 + 建议动作`
  - 副文案：`原因解释；一句轻建议`
  - 小号分数：放在角落或标题附近，不作为视觉主中心
- 白天颜色从低到高：
  - 红
  - 橙
  - 黄
  - 绿
  - 蓝
- 夜间颜色从低到高反向：
  - 蓝
  - 绿
  - 黄
  - 橙
  - 红
- 主卡下方只保留一段总解释区域，不保留当前三张因子卡与贡献图例。
- 详情页不再从总览页进入；第一版不强制删除既有详情文件，只从页面结构中移除入口。

### 5. 文案系统重构

- 现有 `ReadinessTextOutput` 从“总述 + 三条因子解释”改成展示层导向结构，例如：
  - `heroTitle`
  - `heroSubtitle`
  - `summaryLine`
  - `missingHint`
- 文案采用词槽拼装，不做“一档一整句”硬编码。
- 标题规则固定：
  - `状态判断` 来自 `mode + band`
  - `建议动作` 来自 `mode + band`
  - 组合格式：`{状态判断}，{建议动作}`
- 副文案规则固定：
  - 前半句取“主因解释”
  - 后半句取“轻建议”
  - 格式：`{主因解释}；{轻建议}`
- 主因选择规则：
  - 取当前模式下贡献最高的因子
  - 若前两因子贡献差小于阈值（建议 8%），可拼成“双因解释”
  - 缺失数据不作为主因，estimated 因子优先级低于 measured
- 文案词库分 4 层准备：

1. `bandLabel`：5 档状态标签  
   - 白天示例：明显下滑 / 偏低 / 平稳 / 良好 / 饱满
   - 夜间示例：还不太困 / 开始放松 / 有些睡意 / 适合入睡 / 睡意很强

2. `bandAction`：5 档动作建议  
   - 白天示例：先降负荷 / 先补能量 / 维持节奏 / 适合推进 / 可以释放能量
   - 夜间示例：先别硬睡 / 进入放松 / 开始收尾 / 准备入睡 / 尽快休息

3. `factorReason`：按因子和高低区间生成  
   - 白天节律：节律窗口未完全打开 / 当前节律处在可用区间 / 当前节律支持继续推进
   - 白天活动：身体还没完全激活 / 当前活动量足够维持清醒 / 今天活动偏多注意别过冲
   - 白天恢复：昨夜恢复拖累了当前状态 / 昨夜恢复一般 / 昨夜恢复为今天托底
   - 夜间睡眠压力：睡眠压力还弱 / 睡意正在累积 / 睡眠压力已经很强
   - 夜间节律：夜间窗口尚未形成 / 夜间窗口正在形成 / 夜间下行窗口已经形成
   - 夜间消耗：今天消耗不足 / 今天消耗基本到位 / 今天身体消耗已足够

4. `softAdvice`：轻动作建议，避免呆滞  
   - 白天低档：补水、轻走动、先做低认知任务、减少切换
   - 白天中档：维持节奏、暂不加刺激、先清一项任务
   - 白天高档：安排推进任务、适度输出、注意留余量
   - 夜间低档：保持低刺激、先不强迫入睡、避免躺平刷手机
   - 夜间中档：调暗灯光、结束高刺激内容、拉低环境噪音
   - 夜间高档：尽快收尾、进入洗漱/卧室流程、避免再提神
- 为避免呆滞，每个词槽至少准备 3 条可轮换短句；同一请求内按固定优先顺序选，后续可再加随机或轮转机制。
- 缺失数据时：
  - 标题仍然正常输出
  - 副文案优先用可用因子解释
  - `missingHint` 保留，但语气改为轻提示，不抢主卡主体

## 接口与类型调整

- 新增展示层类型：
  - `ReadinessBand`
  - `ReadinessPalette` 或等价颜色 token
  - `HeroCopy` 或扩展后的 `ReadinessTextOutput`
- 调整评分输出类型：
  - 夜间若改掉 `sleepDebtBonus`，同步更新 `ReadinessResult`
  - 若需兼容旧调试/旧页面，可临时保留字段但不再参与总分
- 文案生成入口继续由聚合器统一产出，不把文案规则散落到 View 层。
- UI 层只消费：
  - `mode`
  - `overallScore`
  - `band`
  - `heroTitle`
  - `heroSubtitle`
  - `summaryLine`
  - `missingHint`

## 测试与验收

- 白天活动动态评分
  - `07:30`、步数很低时，不应落入最低档
  - `11:00`、轻微活动应高于刚起床同样步数
  - `16:00`、全天活动不足时，应明显低于上午
  - `19:00`、步数已达合理区间时，应进入中高档
- 夜间正交评分
  - 昨夜短睡、夜间窗口形成、今日消耗到位时，应落高档
  - 昨夜睡足、当前尚早、今日消耗不足时，应落低档
  - 不再出现“基础分一般但 bonus 突然抬高”的解释割裂
- 5 档映射与 UI
  - 白天从低到高颜色为红橙黄绿蓝
  - 夜间从低到高颜色为蓝绿黄橙红
  - 当前档位的大圆正确放大，右侧全部灰色
  - 数字分数为次级元素，不抢标题层级
- 文案
  - 标题始终是“状态 + 建议”
  - 副文案始终是“原因 + 建议”
  - 同档位不同主因时，副文案明显不同
  - 缺失 activity / sleep / weather 时仍能产出自然文案，不出现硬伤
- 回归
  - 模式切换仍按 `22:00-05:59` 夜间、`06:00-21:59` 白天
  - Debug 覆盖仍可强制模式、时间、步数、睡眠进行验证

## 假设与默认值

- 保留白天/夜间双模式，不合并成单一语义分数。
- 总览页第一版采用“主卡 + 一句总解释”的结构，不保留当前三张因子卡。
- 夜间对外语义按“睡眠压力 / 入睡需求”解释，但模块入口名称可以继续沿用“就绪度”；若后续产品要改入口命名，再单独调整。
- 第一版不做个体化基线和用户历史分布校准，先使用固定阈值和固定词库。
- 第一版不做复杂随机文案引擎，只做可控的词槽轮换规则，保证实现简单且不僵硬。
